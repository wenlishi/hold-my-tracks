{
    "configurations": [
        {
            "type": "java",
            "name": "Spring Boot-TrackApplication<track-backend>",
            "request": "launch",
            "cwd": "${workspaceFolder}",
            "mainClass": "com.track.TrackApplication",
            "projectName": "track-backend",
            "args": "",
            "envFile": "${workspaceFolder}/.env"
        }
    ]
}